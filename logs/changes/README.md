# Change Log (Design-Level)

Each entry should capture:
- What changed
- Why it changed
- Impact on agent behavior or constraints

This is distinct from runtime logs and serves as a development ledger.